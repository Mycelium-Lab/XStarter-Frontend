
image: node

before_script:
  - npm install

cache:
  paths:
    - node_modules/

pages:
  stage: deploy
  script:
    - unset CI
    - BASE_PATH=611cdf8b6d5c52001adc348c/ npm run build
    - mv public public2
    - mv build public
  artifacts:
    paths:
      - public
  only:
    - master
  